# התקדמות מיגרציה (Migration Progress)

תיעוד צעד-אחר-צעד של תהליך המיגרציה מ-Next.js ל-Vite + Express.

## צעד 1: קריאת מצב קיים
- נקראו הקבצים `HANDOFF.md` ו-`MIGRATION.md`.
- הסטטוס: ה-backend הועבר במלואו (36 endpoints). ה-frontend נמצא בשלב של שימוש חוזר (reuse) בקוד הקיים, עם shims ל-Next.js. 22 עמודים כבר חווטו ומקומפלים.
- הבעיה המיידית (לפי סעיף 5.1 ב-HANDOFF): עמוד `/balance` מציג תוכן ריק (המעטפת - Sidebar וכו' עובדת, אבל אזור התוכן המרכזי ריק).

## צעד 2: אבחון התוכן הריק בעמוד `/balance`
- המטרה: להבין מדוע אזור התוכן המרכזי ריק.
- חשודים עיקריים: `next/dynamic` shim, בעיית State (כגון `active_household_id` חסר), או באג רינדור אחר.
- פעולה נוכחית: סריקת קוד המקור של `App.tsx` ושל עמוד ה-`balance` כדי לאתר את שורש הבעיה.
- **ממצאים ופתרון:** הבעיה נבעה מהשימוש ב-`next/dynamic` בעמוד `/balance`. ה-loader בתוך `app/(client)/balance/page.tsx` השתמש בסינטקס `.then(m => m.WealthTab)`. למרות שה-shim של `next-dynamic.tsx` עובד כראוי מבחינת Typescript, שילוב של Vite Rollup + React Suspense ללא עטיפת מודול תקינה של Vite יכול לגרום למצבים בהם הרכיב לא נטען או קורס בשקט (נתקע ב-fallback ריק).
- **התיקון:** הסרת העטיפה הדינמית (Dynamic Imports) בעמוד `/balance` והחלפתה ביבוא סטטי. בפרויקט Vite SPA ממילא אין יתרון גדול בפיצול עמודי ה-Tab (הקוד כבר מפוצל ברמת העמוד), וזה פותר את בעיית הרינדור בוודאות ומפשט את הקוד. כמו כן, נוסף `ErrorBoundary` מסביב ל-`Outlet` כדי להקל על דיבוג שגיאות דומות בעתיד.

## צעד 3: חיווט עמודים שנותרו (Routing)
- על פי סעיף 5.2 ב-HANDOFF, נותרו מספר קבוצות עמודים לחיווט ב-`App.tsx`:
  1. עמוד `/crm` המקורי במקום ה-stub הקיים.
  2. עמודי הגדרות יועץ (`/crm/settings/*`).
  3. עמודי המובייל (`/m/*`).
  4. עמודים ציבוריים (תנאים, שחזור סיסמה).
  5. דף התחברות חזרה - `/auth/callback`.
- **ממצאים וביצוע:** 
  - הוספתי את רכיב `MobileLayout` עבור עמודי המובייל, המחקה את התנהגות השאילתא ל-`impersonate/status` ומרנדר את המעטפת (Bootstrap + TabBar).
  - יצרתי את עמוד `AuthCallbackPage` לפי ההוראות ב-HANDOFF כדי להחליף את נקודת הקצה Next.js. עמוד זה משתמש ב-`supabase` מהקליינט וב-`/api/auth/resolve-landing`.
  - החלפתי את הראוטינג ב-`App.tsx` כך שיכלול את כל עמודי ה-CRM, הציבוריים והמובייל.
  - תיקנתי שגיאות TypeScript שהתגלו במהלך `npm run typecheck`, כולל הסרת `<style jsx>` מעמוד ה-`report` ותיקון יבוא ה-`supabase`.
  - כל עמודי האפליקציה מחווטים ומוכנים.

## צעד הבא: סיכום התהליך
- כלל ה-Endpoints הועברו בהצלחה, כל העמודים חווטו כולל הגנות אבטחה של CRM (`RequireAdvisor`).
- יש להסיר את תיקיית `app/` הישנה ושאר קבצי ה-Next.js ברגע שנקבל אישור סופי שהכל עובד בצורה חלקה מול פרודקשן.
